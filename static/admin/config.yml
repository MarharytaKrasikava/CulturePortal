backend:
  name: github
  repo: MarharytaKrasikava/CulturePortal
  branch: netlify
media_folder: static/assets
public_folder: assets
collections:
  - name: blog
    label: Blog
    folder: src/blog
    create: true
    fields:
      - { name: path, label: Path }
      - { name: title, label: Title }
      - { name: image, label: Image, widget: image}
      - { name: body, label: Body, widget: markdown }
